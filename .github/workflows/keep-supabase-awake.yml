name: Keep Supabase Awake

on:
  schedule:
    - cron: '0 */6 * * *'
  workflow_dispatch:

jobs:
  keep-awake:
    runs-on: ubuntu-latest
    steps:
      - run: curl -s -o /dev/null https://muhammad-imran-portfolio-self.vercel.app/
      - run: echo "✅ Keep-alive completed at $(date -u)"